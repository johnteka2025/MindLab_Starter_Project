# Normalize line endings (avoid LF/CRLF warning noise)
* text=auto

# Windows scripts should be CRLF
*.ps1 text eol=crlf
*.psm1 text eol=crlf
*.psd1 text eol=crlf

# Shell scripts should be LF (if any)
*.sh text eol=lf
