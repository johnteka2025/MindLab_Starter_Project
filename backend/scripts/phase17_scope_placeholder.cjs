"use strict";

function getPhase17Scope() {
  return {
    phase: "phase17",
    status: "placeholder",
    tasks: [
      "define scope",
      "add modules",
      "add smoke verification",
      "add reconcile verification"
    ]
  };
}

module.exports = { getPhase17Scope };
