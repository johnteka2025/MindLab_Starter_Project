"use strict";

function getPhase18Scope() {
  return {
    phase: "phase18",
    status: "placeholder",
    tasks: [
      "define scope",
      "add modules",
      "add smoke verification",
      "add reconcile verification"
    ]
  };
}

module.exports = { getPhase18Scope };
