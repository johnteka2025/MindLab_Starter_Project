const path = require('path');

test('progressRoutes module loads and exports a function', () => {
  const modPath = path.join(__dirname, '..', );
  const mod = require(modPath);
  expect(typeof mod).toBe('function');
});
